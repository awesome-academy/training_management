<tr>
  <td><%= l course.start_date, format: :date_month_year_concise if course.start_date %></td>
  <td><%= link_to course.course_name, admin_course_path(course) %></td>
  <td><%= course.trainee_number %></td>
  <td><%= l course.end_date, format: :date_month_year_concise if course.end_date %></td>
</tr>
